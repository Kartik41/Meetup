<%= form_with(model: friend_variant) do |form| %>
  <% if friend_variant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(friend_variant.errors.count, "error") %> prohibited this friend_variant from being saved:</h2>

      <ul>
        <% friend_variant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :phone2 %>
    <%= form.text_field :phone2 %>
  </div>

  <div class="field">
    <%= form.label :friends_id %>
    <%= form.text_field :friends_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
